# Classes that define their own `__str__` and that nothing in the suite ever renders
# (the rendering debt).
#
# An audit once read the `__str__` bodies of this library and found four that dropped the
# field distinguishing two objects from each other. It was an audit and not a gate, so
# renderings went on being added that nothing ever called. `BTH1Allowables` carried five
# allowable stresses, printed three, and called one of the two the standard names "F_t"
# just "F_t" — a reader was shown an unlabelled value and could not tell which, while the
# net-section allowable a lug's net-tension check is judged against was printed nowhere.
#
# The gate in tests/conftest.py holds this as a RATCHET, in the same shape as the
# editionless-citation debt beside it. It fails in three directions: a rendering nothing
# calls that is not on this list, a listed rendering that is now called (strike it off),
# and a listed name that no longer defines a `__str__` at all. The list can only shrink.
#
# Paying one off is one line in a test: build the object and read what it prints against
# what it carries. That is how the four were found.
#
anvilate.agenteval.ToolCall
anvilate.analysis.aluminum.AluminumCompressionStrength
anvilate.analysis.beam.BeamBendingResult
anvilate.analysis.cold_formed_steel.DSMStrength
anvilate.analysis.embodied_carbon.EmbodiedCarbonEstimate
anvilate.analysis.interference.InterferenceFit
anvilate.analysis.pressure_vessel.BranchReinforcement
anvilate.analysis.pressure_vessel.FlangeMoments
anvilate.analysis.pressure_vessel.FlangeShapeFactors
anvilate.analysis.pressure_vessel.NozzleReinforcement
anvilate.analysis.pressure_vessel.ThickWallSphereStress
anvilate.analysis.pressure_vessel.ThinWallStress
anvilate.analysis.rivet.RivetedJointStrength
anvilate.analysis.stress.CombinedNormalStress
anvilate.callouts.CalloutSet
anvilate.callouts.FreeTextNote
anvilate.derivation.Underived
anvilate.standards.fatigue.WeldDetailCategory
anvilate.tolerance.explicit.ResolvedTolerance
anvilate.tolerance.general.GeneralTolerance
anvilate.tolerance.iso286.StandardTolerance
