{
  "$defs": {
    "Quantity": {
      "description": "A physical value: a magnitude and the unit it was expressed in.\n\nConstruct directly (``Quantity(magnitude=75, unit=\"kip\")``) or parse from\ntext (``Quantity.parse(\"75 kip\")``). The stored ``unit`` string is exactly\nwhat the user wrote, canonicalized only to Pint's spelling of it.",
      "properties": {
        "magnitude": {
          "title": "Magnitude",
          "type": "number"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        }
      },
      "required": [
        "magnitude",
        "unit"
      ],
      "title": "Quantity",
      "type": "object"
    }
  },
  "$id": "https://anvilate.dev/schemas/elements/welded_connection/1.0.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Anvilate pack element 'welded_connection': the fields a Design Spec puts in `element_params` when it declares `element_type: welded_connection`. Generated from anvilate.packs.structural.WeldedConnection.",
  "properties": {
    "electrode_strength": {
      "$ref": "#/$defs/Quantity"
    },
    "leg_size": {
      "$ref": "#/$defs/Quantity"
    },
    "load": {
      "$ref": "#/$defs/Quantity"
    },
    "name": {
      "title": "Name",
      "type": "string"
    },
    "signed_fields": {
      "default": [
        "load"
      ],
      "items": {
        "type": "string"
      },
      "title": "Signed Fields",
      "type": "array"
    },
    "weld_length": {
      "$ref": "#/$defs/Quantity"
    }
  },
  "required": [
    "name",
    "leg_size",
    "weld_length",
    "load",
    "electrode_strength"
  ],
  "title": "WeldedConnection",
  "type": "object",
  "x-anvilate-version": "1.0.0"
}
