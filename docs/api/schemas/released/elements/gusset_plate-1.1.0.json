{
  "$defs": {
    "Quantity": {
      "description": "A physical value: a magnitude and the unit it was expressed in.\n\nConstruct directly (``Quantity(magnitude=75, unit=\"kip\")``) or parse from\ntext (``Quantity.parse(\"75 kip\")``). The stored ``unit`` string is exactly\nwhat the user wrote, canonicalized only to Pint's spelling of it.",
      "properties": {
        "magnitude": {
          "title": "Magnitude",
          "type": "number"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        }
      },
      "required": [
        "magnitude",
        "unit"
      ],
      "title": "Quantity",
      "type": "object"
    }
  },
  "$id": "https://anvilate.dev/schemas/elements/gusset_plate/1.1.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Anvilate pack element 'gusset_plate': the fields a Design Spec puts in `element_params` when it declares `element_type: gusset_plate`. Generated from anvilate.packs.structural.GussetPlate.",
  "properties": {
    "load": {
      "$ref": "#/$defs/Quantity"
    },
    "material": {
      "title": "Material",
      "type": "string"
    },
    "name": {
      "title": "Name",
      "type": "string"
    },
    "net_shear_area": {
      "$ref": "#/$defs/Quantity"
    },
    "net_tension_area": {
      "$ref": "#/$defs/Quantity"
    },
    "signed_fields": {
      "default": [
        "load"
      ],
      "items": {
        "type": "string"
      },
      "title": "Signed Fields",
      "type": "array"
    }
  },
  "required": [
    "name",
    "net_shear_area",
    "net_tension_area",
    "load",
    "material"
  ],
  "title": "GussetPlate",
  "type": "object",
  "x-anvilate-version": "1.1.0"
}
