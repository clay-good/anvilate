{
  "$defs": {
    "Quantity": {
      "description": "A physical value: a magnitude and the unit it was expressed in.\n\nConstruct directly (``Quantity(magnitude=75, unit=\"kip\")``) or parse from\ntext (``Quantity.parse(\"75 kip\")``). The stored ``unit`` string is exactly\nwhat the user wrote, canonicalized only to Pint's spelling of it.",
      "properties": {
        "magnitude": {
          "title": "Magnitude",
          "type": "number"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        }
      },
      "required": [
        "magnitude",
        "unit"
      ],
      "title": "Quantity",
      "type": "object"
    }
  },
  "$id": "https://anvilate.dev/schemas/elements/worker_noise_exposure/1.1.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Anvilate pack element 'worker_noise_exposure': the fields a Design Spec puts in `element_params` when it declares `element_type: worker_noise_exposure`. Generated from anvilate.packs.noise_exposure.WorkerNoiseExposure.",
  "properties": {
    "exposure_duration": {
      "$ref": "#/$defs/Quantity"
    },
    "machine_levels": {
      "items": {
        "type": "number"
      },
      "title": "Machine Levels",
      "type": "array"
    },
    "signed_fields": {
      "default": [],
      "items": {
        "type": "string"
      },
      "title": "Signed Fields",
      "type": "array"
    }
  },
  "required": [
    "machine_levels",
    "exposure_duration"
  ],
  "title": "WorkerNoiseExposure",
  "type": "object",
  "x-anvilate-version": "1.1.0"
}
