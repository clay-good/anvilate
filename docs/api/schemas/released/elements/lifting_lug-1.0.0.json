{
  "$defs": {
    "Quantity": {
      "description": "A physical value: a magnitude and the unit it was expressed in.\n\nConstruct directly (``Quantity(magnitude=75, unit=\"kip\")``) or parse from\ntext (``Quantity.parse(\"75 kip\")``). The stored ``unit`` string is exactly\nwhat the user wrote, canonicalized only to Pint's spelling of it.",
      "properties": {
        "magnitude": {
          "title": "Magnitude",
          "type": "number"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        }
      },
      "required": [
        "magnitude",
        "unit"
      ],
      "title": "Quantity",
      "type": "object"
    }
  },
  "$id": "https://anvilate.dev/schemas/elements/lifting_lug/1.0.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Anvilate pack element 'lifting_lug': the fields a Design Spec puts in `element_params` when it declares `element_type: lifting_lug`. Generated from anvilate.packs.structural.LiftingLug.",
  "properties": {
    "hole_diameter": {
      "$ref": "#/$defs/Quantity"
    },
    "load": {
      "$ref": "#/$defs/Quantity"
    },
    "material": {
      "title": "Material",
      "type": "string"
    },
    "name": {
      "title": "Name",
      "type": "string"
    },
    "signed_fields": {
      "default": [
        "load"
      ],
      "items": {
        "type": "string"
      },
      "title": "Signed Fields",
      "type": "array"
    },
    "thickness": {
      "$ref": "#/$defs/Quantity"
    },
    "width": {
      "$ref": "#/$defs/Quantity"
    }
  },
  "required": [
    "name",
    "width",
    "hole_diameter",
    "thickness",
    "load",
    "material"
  ],
  "title": "LiftingLug",
  "type": "object",
  "x-anvilate-version": "1.0.0"
}
