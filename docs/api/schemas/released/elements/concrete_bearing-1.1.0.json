{
  "$defs": {
    "Quantity": {
      "description": "A physical value: a magnitude and the unit it was expressed in.\n\nConstruct directly (``Quantity(magnitude=75, unit=\"kip\")``) or parse from\ntext (``Quantity.parse(\"75 kip\")``). The stored ``unit`` string is exactly\nwhat the user wrote, canonicalized only to Pint's spelling of it.",
      "properties": {
        "magnitude": {
          "title": "Magnitude",
          "type": "number"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        }
      },
      "required": [
        "magnitude",
        "unit"
      ],
      "title": "Quantity",
      "type": "object"
    }
  },
  "$id": "https://anvilate.dev/schemas/elements/concrete_bearing/1.1.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Anvilate pack element 'concrete_bearing': the fields a Design Spec puts in `element_params` when it declares `element_type: concrete_bearing`. Generated from anvilate.packs.structural.ConcreteBearing.",
  "properties": {
    "bearing_area": {
      "$ref": "#/$defs/Quantity"
    },
    "concrete_strength": {
      "$ref": "#/$defs/Quantity"
    },
    "load": {
      "$ref": "#/$defs/Quantity"
    },
    "name": {
      "title": "Name",
      "type": "string"
    },
    "signed_fields": {
      "default": [
        "load"
      ],
      "items": {
        "type": "string"
      },
      "title": "Signed Fields",
      "type": "array"
    },
    "support_area": {
      "$ref": "#/$defs/Quantity"
    }
  },
  "required": [
    "name",
    "bearing_area",
    "support_area",
    "concrete_strength",
    "load"
  ],
  "title": "ConcreteBearing",
  "type": "object",
  "x-anvilate-version": "1.1.0"
}
